# StudyPlan 開発の入口

- 最新のユーザー指示を優先。最初にGitの状態・差分・関連コード・テストを確認。リモート調査ではfetchし、共有済みと確認不能を区別する。
- 未コミット変更を保護。無断のreset・clean・強制push・競合上書きは禁止。コミット名だけで実装済みと判断しない。
- UI・進捗を変える前に[プロダクト契約と検証経路](docs/ARCHITECTURE.md#進捗と画面の契約)を読む。同じ意味と主要操作を画面ごとに再実装せず、共通selector・既存記録画面を使う。
- 日本語でコンパクトに。WCAG 2.2 AA、既存テーマ、ネイティブHTMLを維持。小さい文字・説明追加だけで問題を隠さない。実画面を広幅・狭幅・キーボードで確認。
- デスクトップの正本はSQLite、デモは分離したブラウザー保存。読込失敗時に初期化しない。互換性・競合・二重送信防止・承認と下書きの区別を壊さない。
- 計画・進捗の制約は[実装仕様](docs/SPEC.md)と[責任分離](docs/ARCHITECTURE.md)へ。UI切替で計画・実績を書き換えない。
- 検証は架空データと専用保存先のみ。通常利用のSQLite・バックアップを試験に使わない。実データ調査は読み取りのみ。
- 完了前に `pnpm verify`、UI・進捗変更では `pnpm verify:ui`。保存/ネイティブ変更では `pnpm test:ui`、Rust変更では関連Cargoテストも行う。未実施を明記し、[動作確認記録](docs/VALIDATION.md)を更新。
- 実機E2Eは `.test-data/` の専用SQLite。releaseはテスト保存先指定を無視するので、通常アカウントで配布EXEの試験を起動しない。
- 親担当は解釈・実装統合・検証・報告・依頼された公開を担当。レビュー担当は既定で読取りのみ、根拠付きの回帰・互換性・UI指摘を返す。
- サブエージェントはユーザーの依頼と実ツールがある場合のみ。担当を分け、同時編集を避ける。利用不可なら自己レビューと明記。承認・セキュリティ設定を緩めない。Skillは確認して使い、同名を無断上書きしない。
- Gitへデータ・秘密・ログ・配布物・テスト出力・依存物を含めない。`.gitignore`を確認。新規ブランチは `codex/`、コミットは日本語。
- 仕様は既存docsへ統合し、版別変更は[CHANGELOG.md](CHANGELOG.md)。READMEに履歴を増やさない。
- 公開は依頼された範囲で[配布手順](docs/RELEASING.md)に従う。コメントは利用者向けに短く、検証結果は載せない。EXEと更新ZIPのみ配布。CMD・独立SHA256SUMSは生成しない。manifest検証は維持。
- 実装・検証まで進め、日本語で簡潔に報告。確認不能な別端末・未実行テストを確認済みにしない。
