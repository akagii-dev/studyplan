# 変更履歴

READMEに掲載していた変更履歴を移しました。各版の記載は公開当時の内容です。

## v0.4.20 の変更

- ホームから「今日のスケジュール・時間内訳」を開き、1日の可処分時間の円グラフを確認できるようにしました。
- 実績記録後の予定変更一覧で、問数が変わる行の時刻一覧を省きました。配置だけが変わった行では移動時刻を確認できます。

## v0.4.19 の変更

- 部分実績で今日の残りを翌日にも配る問題を修正。既存の配置を保持して必要な残量だけ調整し、数量変更と配置のみの変更を分けて表示します。

- 今日・今後の予定・カレンダー・週間レポートの進捗表示を統一。未報告と0問記録を区別します。
- 今日に小さな進捗円を追加し、詳細から対象行へ安全に入力を引き継ぎます。
- カレンダーの固定切替ボタンを削除しました。
- 今後の配布からCMD・SHA256SUMSを外し、READMEの版別履歴を本ファイルへ移しました。
- 週間レポートは保存済み予定と実績を参照し、異なる単位を分けて集計します。
- 進捗の意味を共通の計算・自動テストで守り、画面変更時の検証を容易にしました。

## v0.4.4 の変更

往路・復路それぞれの出発時刻と所要時間を対話で確認します。食事と重なる場合は修正を案内し、食事を自動で移動しません。一括スキップボタンを削除し、サイドバー開閉レールを12pxにしました。旧設定で授業から自動計算していた出発時刻は、通学設定で再確認してください。既存の計画へ新しい計算を反映するには、再計画案を確認して承認してください。

## v0.4.5 の変更

「1日の可処分時間」を、学習可能量・配分バー・簡潔な凡例・折りたたみ詳細に整理しました。往復の通学時刻は詳細で確認できます。修正が必要な警告はそのまま表示します。計算・保存データは変更しません。全テーマ、キーボード操作、文字拡大と狭い表示の検証結果は [VALIDATION.md](docs/VALIDATION.md) を参照してください。

## v0.4.6 の変更

「1日の可処分時間」の配分バーをドーナツ型の円グラフへ変更しました。広い画面では主指標・円グラフ・凡例を横に、狭い画面では縦に配置します。説明文は追加せず、計算・保存・詳細・警告は維持します。

## v0.4.7 の変更

円グラフの通学・食事を一つの区分と凡例にまとめ、睡眠の色を控えめにしました。詳細の分類名に対応色の下線を付けました。初期設定に任意の睡眠・風呂を追加しました。設定項目から再編集でき、学習対象外・未設定の部分だけを細分化して表示します。学習計画の計算は変わりません。

## v0.4.8 の変更

円の中心に学習可能時間を表示し、外側の主指標と凡例の重複を削除しました。円グラフと内訳を2列に整理し、狭い幅では縦に並べます。文字拡大で円内へ収まらない場合は、値を円の直下へ移して重なりを防ぎます。計算・保存データは変更しません。

## v0.4.9 の変更

今日のスケジュールの時刻の内訳から、未設定の区間へ好きな名前を付けられます。日付ごとの表示名なので、学習量の計算は変わりません。初期設定の項目選択では、選んだ項目だけを修正して終了できます。

カレンダーは授業と試験別の日合計を並べ、個別予定は日付選択や詳細の展開で確認できます。円グラフの睡眠・風呂も一つの区分と凡例にまとめました。

## v0.4.10 の変更

未設定区間の名称編集をペンアイコンに整理しました。計画作成時の必須入力エラーは、設定保存後に再検証し、解消した項目だけを除外します。

## v0.4.11 の変更

編集アイコンの有無にかかわらず、時刻内訳の分類名を文字位置で揃えました。変更したウィンドウサイズを保存し、次回起動時に復元します。画面より大きい保存サイズだけを表示可能な範囲へ補正します。

GitHub Pagesで公開デモを提供します。デモのデータはブラウザー内だけに保存し、デスクトップ版とは分離します。書き出し機能はデモの対象外です。

## v0.4.12 の変更

ウィンドウサイズの保存・復元を修正しました。最大化して閉じた場合も状態を引き継ぎます。追加プラグインは使わず、アプリ本体で処理します。

## v0.4.14 の変更

進捗の訂正・取消後に表示する見直し通知を、承認済み計画に実績が取り込まれたかで判定します。承認後と再起動後は不要な通知を表示せず、案の作成・破棄だけでは反映済みにしません。

再計画では、教材・周回の予定量、開始・終了予定、主な日別増減、未配置を比較します。全日別差分は詳細から確認できます。

## v0.4.16 の変更

「今日」で問題集ごとの予定と実績を並べ、その場で実績を記録すると明日以降を自動で再配分します。設定画面は未登録を操作できる一覧にまとめ、補足条件は必要なときだけ開けます。削除の確認と読込失敗からの再試行も改善しました。

公開デモは初回だけ架空の試験・問題集と計算済み予定を表示します。ブラウザー内の既存データは保持します。

## v0.4.17 の変更

今後の予定に実績と過去の不足を表示します。未報告と0問記録を区別し、詳細カレンダーの常設一覧を整理しました。

## v0.4.18 の変更

既存の計画・履歴から過去の予定量を取得する処理を修正しました。今後の予定は未報告と0問記録を区別し、元の予定を取得できない場合は実績だけを表示します。
