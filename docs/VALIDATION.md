# 動作確認記録

実施日：2026-09-20。Windows x64、Node.js 24.19.0、Rust 1.98.1、Tauri 2.11.5、React 19.3.0、rusqlite 0.32.1。

## 固定エラーの理由・修正操作、授業名の順序、ICS

- TypeScriptの追加11件で、固定予定が授業・食事・学習枠の外・連続上限・休憩・余裕率に当たる場合の原因判定、対象の質問への移動、候補条件と実績を保った固定解除を確認した。
- `17.45時`が17:27を表す小数表示だった不具合を再現し、HH:MMの開始・終了で表示するよう修正した。当日の開始済みの未固定予定まで「固定予定」のエラーとして扱っていた処理も再現した。開始済みの予定は元の時刻のまま保持し、新しい条件との違反で承認を妨げず、未報告の確認は維持する。
- ユーザーの実データでどの制約が該当したかは取得できなかった。テスト用の旧計画と変更後の条件で再現した結果である。
- 授業名欄の月曜始まり・時刻順、元の配列の非変更、IDに対応した保存を確認した。ICSは授業期間・曜日・試験フィルター・承認済み計画だけの出力、24:00、安定UID、日本語・絵文字・改行等の往復、75バイトの折り返し、空欄・不正入力の拒否を独立した `ical.js` パーサーで確認した。
- Rust追加1件で、日本語ファイル名へのICS出力と既存ファイルの原子的な置換、不正内容・拡張子・書込先の場合の拒否と既存ファイルの保護を確認した。
- Windows実画面では、授業名の並びと編集、ICSの入力エラー・保存取消・両方／授業のみ／特定試験の学習のみの実ファイル保存を検証。食事や未承認案が混ざらず、出力操作で設定・計画・実績が変わらないことを確認した。
- 固定予定と経済学の授業が重なる案を使い、理由表示・承認無効化・修正ボタンから入力済みの授業開始時刻への移動を確認した。対象の固定だけを解除し、候補の余裕率10%・他の固定・0問の記録を保ち、承認前は元の設定・時刻、承認後は候補設定と新しい計画になることを確認した。
- 最初の実画面テストは制限付き起動でWebView2の接続ができず、操作前に中断した。通常の実行権限でテスト専用保存先・画面データを使うと、10件すべて成功（1.2分）。画面画像 `test-results/class-name-order.png`、`calendar-export.png`、`constraint-repair.png` を確認した。

## 自動テスト

| 検証                                              | 結果                                  |
| ------------------------------------------------- | ------------------------------------- |
| TypeScript 型検査 + Vite本番ビルド                | 成功                                  |
| `pnpm test`                                       | 133件成功                             |
| `cargo test --manifest-path src-tauri/Cargo.toml` | 13件成功                              |
| `pnpm test:ui`                                    | Windows実機E2E 16シナリオが一括で成功 |
| Tauri Windows releaseビルド                       | 成功                                  |

追加の未設定チェック8件、授業除外・条件保存・数値確定12件を含め、計算・状態遷移テストでは以下を確認した。

- 100分の授業と重複予定を一度だけ除外し、計画の全セッションが授業外になること。
- 授業の適用期間外では差し引かず、当時の設定の写しと更新日時を計画に保持すること。
- 授業を追加した後の古い案は承認できず、新案で授業を除くこと。
- 余裕率0%の参考計算も連続学習の長さ・固定・共有枠を守り、元の設定や実績を変更しないこと。
- 空欄・文字列・指数表記・非整数の拒否、0・3・7問と小数分の確定、上下限の検証。

- 「予定なし」「あとで設定」「未確認」を区別し、各項目の影響と解決方法を表示する。
- 試験・教材・学習枠の未登録は必須エラーとし、任意項目の注意では容量を勝手に変更しない。
- 延期後に一部だけ入力しても注意を残し、確認完了で解消する。確認状況を変えても実績・承認済み計画は変更しない。

- 学習可能枠と予定の重複を結合し、二重に加算・減算しない。
- 授業の適用期間、終日不可、時間帯不可を反映する。
- 空き枠、連続学習の長さ・休憩後、余裕率適用後を区別する。
- 複数試験で共有枠を重複使用せず、連続学習の長さと余裕率を守る。
- 期限・優先度・教材順序を考慮し、複数日に分散する。
- 枠の長さが日によって違う場合も、容量に応じて配分する。
- 3問・7問を含む整数の問題数を保存し、未配置分と配置済み分の和が残数に一致する。
- 周回別の推定時間、独立した復習期間、1問がブロックを超える場合を扱う。
- 多量の通常課題があっても、他試験の復習期間の枠を確保する。
- 残数超過、負数、小数、重複記録IDを拒否する。
- 0問と未報告を区別し、0問で既存完了数を消さない。
- 訂正と取消で累計・残数を再計算する。
- 再計画の承認前に元計画を変えず、未報告確認を必須にする。
- 固定予定と完了記録を保持し、固定が枠外・残数超過・変更後の期限外なら承認を止める。
- 当日の経過済み時刻に新しい課題を置かない。
- 計画を戻しても進捗は戻さない。
- 再計画できない設定でも進捗記録自体は保持する。

Rustテストは一時SQLiteファイルを実際に作成し、接続を閉じて再度開き、設定・入力途中・計画・進捗が一致することを確認。二重要求、古いリビジョンの書き込み、残数超過時のトランザクション拒否、取消済み記録・0問・監査履歴も確認した。

## Windows実画面のE2E

モック保存やブラウザー内メモリーではなく、**Tauri実行ファイル + WebView2 + Rust + SQLite**で実施した。データは`.test-data/native-*`に分離した。

1. 一問ずつの初期設定で、未入力時の「次へ」が無効になることを確認。
2. 試験名を途中入力して再読み込みし、同じ質問と回答に復帰。
3. 前の質問へ戻って回答を確認。
4. 2試験・2教材・曜日別枠・授業・集中量・余裕率を登録。
5. 初回計画案を作成し、承認。
6. 月・週・一覧表示を切り替えて、統合計画IDが変化しないことを確認。
7. 試験別表示で他試験の予定が隠れることを確認。
8. 予定を固定。
9. 「その他」で3問を二重クリック送信し、1件だけ記録。
10. 0問を記録し、累計3問が維持されることを確認。
11. 履歴で3問を7問に訂正。
12. 再計画を承認し、承認前計画へ戻す。進捗7問は維持。
13. アプリプロセスを終了して再起動。2件の記録が復元されることを確認。
14. 7問の記録を取消。残り37問に戻ることを確認。
15. 「残りすべて：37問」を登録し、残数0と5問ボタンの無効化を確認。

追加の実画面検証では、延期前の影響説明、最終確認・計画案の注意、アプリ再起動後の延期状態の保持、設定画面での確認完了と注意の解除も確認した。

今回の更新では次も検証した。

- 初期設定の授業セルに「学習不可」が表示される。
- 数値欄を全消去しても確定済みの連続時間50分が0に変わらない。空欄のままでは「次へ」でエラーを表示する。
- 空欄のまま保存・再読み込みしても空欄が復元される。入力中はエラーにせず、Enterまたは次へで空欄・小数を検証する。120分をEnter1回で確定するとそのまま次の質問に進む。
- 計画案の設定写し・日時、余裕率0%の参考終了日欄が表示される。
- 学習枠09:00〜13:00と18:00〜21:00、月曜の授業09:00〜10:40と昼食12:00〜13:00・夕食19:00〜19:45で、除外後が10:40〜12:00・18:00〜19:00・19:45〜21:00、合計3時間35分になる。
- 時間枠変更後は古い計画との不一致を表示する。
- ミント・ペールブルー・ライムを切り替え、設定更新日時が変わらず、プロセス終了・再起動後もライムを保持する。

追加の画面画像：`test-results/timetable-exclusion.png`、`theme-mint.png`、`theme-sky.png`、`theme-lime.png`。初回検証では開発サーバーのCSS読込が完了せずタイムアウトしたが、再実行では全操作を通過した。配布版は別途、同梱画面で起動を確認した。

画面画像：`test-results/setup-question.png`、`setup-skip-impact.png`、`setup-finish.png`、`calendar.png`、`progress.png`、`dashboard.png`。

## 授業の重複表示と対話による再計画の追加検証

`tests/revision.test.ts`で10件を追加。設定の独立した下書き、承認前の不変性、承認と計画だけの復元、固定と実績の保持、他画面からの設定・確認状態の変更検出、記録後の候補条件の維持、完了数の保護、授業前後の移動、日付をまたぐ除外を確認した。

画像の10:40〜10:49・12:30〜13:18・13:28〜13:58という学習予定に、テスト用の09:00〜10:40・10:50〜12:30・13:10〜14:50の授業を組み合わせ、後ろ2件の衝突検出と再計画による解消を確認した。元画像の保存データ自体は取得できなかったため、この組合せによる再現検証である。

実機E2Eではテスト専用SQLiteに衝突する旧計画を置き、以下を確認した。

- 衝突2件の警告、授業と学習の時刻順表示、衝突相手の時刻の表示。
- 対話に現在の設定が入力済みで表示され、目標日・連続時間・食事・授業前後を変更できる。
- 質問途中で再読み込みして続行でき、元の設定は変わっていない。
- 案の作成・破棄では設定・計画・実績を変更しない。
- 承認すると設定と計画が更新され、全学習予定が授業と重ならず、固定予定と完了記録を保持する。
- プロセス再起動後も承認結果と実績が残る。

画面画像：`test-results/calendar-conflict.png`、`revision-dialogue.png`、`calendar-resolved.png`。

## 食事・連続時間・可処分時間・Enter操作の更新

追加の `tests/dailyTime.test.ts` 13件で、食事と授業の重複除外、日付をまたぐ食事、120分学習→30分休憩の反復、旧日次上限の不使用、休憩0分の拒否と日付をまたぐ休憩、24時間の内訳の合計、未設定時間の扱い、食事30〜60分の検証、旧案の承認拒否、承認・取消時の固定予定と記録の保持を検証する。

実機E2Eは、朝昼夜の時刻・長さ、空欄時刻のエラー、数値のEnter1回での前進、入力後に「次へ」をクリックして確定する操作、変換中Enterイベントの無視、再計画の食事変更と承認までの不変性を確認する。日本語IME自体の手動操作は未検証。月・週・一覧のカレンダーで食事や移動カードが出ず、授業カードが残ることを確認する。

テーマの固定された緑を文字色・装飾・進捗の本のイラストから除き、共通変数に統一。試験に設定した色、食事の区分色、警告・エラー色は役割別に維持。各テーマのホーム・進捗画面画像を `test-results/theme-*.png`、`test-results/progress-*.png` に保存する。

## まとまり・初期設定の再編集・今日の予定

追加の tests/groupedSessions.test.ts 13件では、37問を30日に細分化しないこと、7問の端数統合、短い推定時間のまとまり、狭い枠から翌日への繰り越し、最後の3問、期限例外、固定・不足、複数試験・教材順序・枠の組合せ30通り、初期化の範囲と1世代の復元を検証した。

実機操作を1シナリオ追加し、任意の授業名、時間割期間の変更、試験目標の再編集、設定編集での計画・実績の保持、今日の予定からの記録画面への移動、初期化の未確認時無効化・取消・質問だけの初期化・全初期化と再起動後の復元を確認する。画面画像は test-results/today-schedule.png と test-results/reset-warning.png。

開発サーバー初回読み込みで一度待機タイムアウトが発生し、再起動テストでは古い画面への参照が残るテストコードを修正した。最終結果を下の配布版確認と合わせて記録する。

## 配布用実行ファイルの起動結果

`node scripts/release-smoke.mjs src-tauri/target/release/studyplan.exe`は成功。新しい実行ファイルを起動し、`http://tauri.localhost/`の同梱画面、ネイティブSQLite読み込み、通常のウィンドウ終了によるプロセス終了を確認した。開発サーバーは使用していない。既存データを変更しない読み取りと画面遷移・終了だけで行った。この時点の配布ファイルは2026-09-20 20:32:50更新、17,112,064バイト（最新版は末尾のv0.2.0を参照）。利用中でないことを確認して `release/StudyPlan.exe` と `release/StudyPlan-20260920.exe` の両方を更新し、起動確認した元ファイルとSHA-256が一致することを確認した（`B1391EF8B2B3FDC649C4F820C0CEF18A1C43C11E3D95A363AE99CD14512564B7`）。

リリース版の初期設定画面画像は`test-results/release-initial-setup.png`。

## 残る制約

- 食事は現在、毎日共通の時刻。曜日ごとの食事の変更は対象外。可処分時間は登録した学習枠内で計算する。
- Windowsで検証した。他OS向けのビルド・実機検証は未実施。
- 計画はヒューリスティック。全ての制約を満たす数学的な最適配置は保証しない。未配置量を表示する。
- インストーラー、署名、自動更新は今回の対象外。実行ファイルの起動にWebView2 Runtimeが必要。
- 長期・大量データでの性能測定、停電やディスク故障の実機試験は未実施。永続化にはSQLiteのトランザクションとWALを使用。
- 起動時に破損したDBを検出して案内するが、物理的な破損の自動修復は行わない。
- 任意の時刻へドラッグする編集、試験・教材削除は未実装。詳細は[SPEC.md](SPEC.md)。
- バックアップのネイティブ保存先ダイアログは選択結果をテスト側で差し替えており、ダイアログそのもののOS操作は自動検証していない。ファイルへの書き込み、読込検証、SQLite復元は実処理で確認。
- ICSの保存先ダイアログも選択結果を差し替えて検証した。Tauri経由の実ファイル出力と独立したICSパーサーでの読込は確認したが、外部カレンダー製品への実際の取込は未検証。取込後の自動更新・重複回避は保証しない。
- 読みやすさの自動検査とキーボード操作を一部画面で確認したが、スクリーンリーダー実機や、全画面・全状態を通したWCAG適合の監査は未実施。
- 週間レポートの全体進捗・予定は出力時点の状態。過去週の終了時点や週初の計画を再現した比較ではない。Markdownの保存先ダイアログは選択結果を差し替え、実ファイルの内容・再起動後の集計はネイティブ処理で確認した。

## 週間レポート・承認前の確認

週間レポートの計算をUIから分離し、TypeScript8件を追加。月曜〜日曜・年またぎ、複数試験と複数周回、3問・7問の集計、分割セッションの重複防止、初期完了数、記録対象日と出力時点の区別、訂正・取消、0問と未報告、未来予定、計画なし・分母0・100%超、未承認案の除外、旧計画との設定差、Markdown記号とHTMLのエスケープを検査した。全117件成功。

Rustの追加1件で、日本語・絵文字を含むMarkdownのUTF-8保存、既存ファイルの置換、内容・拡張子・保存先不正の場合の拒否と元ファイル保持を確認。全13件成功。最初は制限付き実行で既存の依存パッケージへのアクセスを拒否されたため、通常権限で再実行した。

実画面では、先週の記録10問・週間予定26問・出力時点の全体23/94問を表示し、実ファイルが同じ値になることを確認。教材の2周分、初期完了数・週外の実績を含む全体比較、0問と取消後の未報告、保存取消、二重クリック、不正拡張子による失敗、出力前後のSQLite状態の不変、アプリ再起動後の再集計を検証した。900×640の画面画像 `test-results/weekly-report.png` と、テスト用に書き出したMarkdown本文を確認した。

読みやすさ検査は週間レポートを追加し、3テーマ×主要6画面と計画表の末尾、計21状態で違反0件。小さい画面で横にはみ出さないことも確認した。表は必要に応じて表内でスクロールできる。

再計画は競合1件と未報告1件を持つテストデータで、承認ボタンの近くに両方の理由が出ること、対応ボタンから各見出しへ移動・フォーカスできることを確認。対象の固定だけ解除しても未報告の確認までは承認できず、明示確認後に承認ボタンへ戻れる。実績・他の固定・未承認の候補条件の保持も通過した。

通しE2Eは13件中12件成功し、従来の一連操作1件が「今日に必ず予定がある」というテスト手順の前提で停止した。夜の実行では当日残り枠に収まらず翌日へ配置され、今日の詳細には固定ボタンがなかった。実際の予定カードを選んでから固定する手順へ直し、対象1件の再実行は成功（18.5秒）。製品の制約は変更していない。型検査、本番ビルド、配布版の起動・SQLite読込・正常終了も成功した。

## 日常利用での表示と保存の追加検証

### 画面移動後の表示位置とキーボード操作

900×640の画面で、時間枠・時間割を末尾までスクロールしてから「今日のスケジュール」へ移ると、見出しが表示領域の外に隠れることを実機テストで再現した。修正後は画面の切替と最初の読込時だけ先頭へ戻し、見出しにフォーカスを移す。

既存のチュートリアル実機テストを拡張し、サイドバーのクリック、Enterでの画面切替とTabによる内容への移動、チュートリアル内の9個の遷移ボタンで見出しが表示されフォーカスされることを確認した。試験名を入力・自動保存しても入力欄のフォーカスとスクロール位置が維持され、各画面を巡回しても保存された設定・入力途中・実績を変更しないことを確認した。修正前は見出しの表示判定で失敗し、修正後の対象テストは成功（6.9秒）。画面画像は `test-results/navigation-arrival.png`。

その後の全12件の実機E2Eは11件成功・1件が未報告の確認待ちで停止した。テスト用SQLiteでは競合0件、未報告は当日19:58開始の簿記1件で、承認ボタンの無効化は仕様どおりだった。通し操作に未報告を保留する確認を追加し、承認後も実績が変わらないことを検査した。古い配色の固定値を期待していた検査も、3テーマの色が互いに異なることと既存の読みやすさ検査へ整理した。該当1件の再実行は成功（18.2秒）。合計12シナリオの確認を完了したが、手順修正後の再実行対象は失敗した1件のみである。型検査・本番ビルド・Windows配布版の起動と終了も成功した。計算処理とRustは変更せず、単体109件・12件は直前の結果を維持する。

### 全体のコントラスト調整

淡いミント・ブルー・ライムを維持し、本文・補助文字、入力欄とボタンの枠線、カードの区切り、警告文を濃くした。背景と白いカードの差、選択中のサイドバー・選択肢・カレンダー表示切替も強調した。無効なボタンの半透明表示をやめ、文字を読める濃さで残す。

変更後に `pnpm test:ui --grep 3テーマ` を実行し、対象の実機E2E 1件が成功（18.3秒）。3テーマ×主要5画面と表の末尾、合計18状態で自動検出された違反は0件。要確認の装飾とスクロール外の行は下記と同じで、表の末尾は追加検査した。900×640での横幅、色見本の保持、表のキーボード操作も通過した。`test-results/contrast-*-home.png`、`contrast-*-progress.png`、`contrast-*-calendar.png` と再計画の画像で配色を確認した。型検査・本番ビルド・Windows配布版ビルドも成功した。

今回の変更はCSSと画面画像の取得・文書のみ。上の全12件の実機E2E、TypeScript109件・Rust12件は直前の版の結果で、今回の配色調整では全件の再実行はしていない。

### 配色・文字・キーボードによる表の操作

開発専用の `@axe-core/playwright` 4.13.0を追加し、ミント・ペールブルー・ライムの各テーマでホーム、学習カレンダー、進捗記録、再計画、記録履歴を検査した。実際のWebView2へ接続し、WCAG A/AAの自動検査を行う。ネイティブの単一ウィンドウで結果集約用の別タブを作らないためLegacyModeを使用し、ルールは除外していない。

最初の検査で、警告文（2.67〜4.00）、薄い背景上の補助文字（4.30〜4.47）、前後の月の半透明な予定、明るい試験色をそのまま使った詳細の文字、取消ボタンのコントラスト不足を検出した。日別問題数のスクロール領域にもキーボードで入れない問題があった。本文や小さい文字の4.5:1という確認基準は[W3Cの説明](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html)を参照する。

修正後の3テーマ×5画面と、表を末尾へスクロールした状態の追加検査で、自動検出された違反は0件。最初の判定で「要確認」となったホームの星印は意味を持たない装飾として確認し、表のスクロール外の行は末尾へ移動して追加検査した。自動検査は全面的なアクセシビリティ適合の保証ではない。

- Tabで日別問題数の表に入り、矢印キーでスクロールし、Endキーで末尾へ移動できる。列見出しと濃いフォーカス枠を画面画像で確認した。
- 選択中の色見本の背景が消える不具合を修正し、編集開始時のオレンジ、選び直したブルーが指定色で表示され、選択状態が伝わることを確認した。
- 900×640の表示領域で、初期設定、時間枠・時間割、カレンダー、進捗記録、記録履歴が画面全体の横スクロールを発生させないことを確認した。
- サイドバーの現在ページ、カレンダーの表示切替、進捗バーの対象名を支援技術向けに明示した。計画計算と保存データの形式は変えていない。

検査結果は `test-results/accessibility.json`、画面画像は `readability-mint.png`、`readability-sky.png`、`readability-lime.png`、`accessibility-small.png`。既存の操作も含む12件の実機E2Eが成功（2.1分）。型検査と本番ビルドも成功。

### 保存の確認と再試行

保存要求とその後の再読込を両方失敗させるテストを追加し、未保存の入力を表示したまま「保存済み」が残る不具合を実画面で再現した。修正後は「保存未確認」と確認画面を出し、編集・通常終了を止める。同じシナリオで以下を確認した。

- 再読込が繰り返し失敗しても確認画面に留まり、画面の入力を追加保存しない。通常の終了要求でもプロセスは残る。
- 保存要求が実行されなかった場合、再読込成功後は直前のSQLiteの内容に戻り、アプリを再開できる。
- SQLiteへの保存は成功して応答だけを失った場合、再読込で実際に保存された新しい内容を表示する。失敗したと決めつけて以前のデータを書き戻さない。
- 「保存を確認せずに終了」の確認で「戻る」を選ぶとプロセスは残り、「終了する」では閉じる。再起動後も確定済みデータは維持され、未保存の入力だけが残らない。

エラーはテスト側で保存コマンドの応答と読込を制御して再現し、SQLiteへの成功した書き込みと再起動は実処理で確認した。物理的なディスク障害試験ではない。画面画像 `test-results/save-recovery.png` を確認。部分実行で一つのテストが前ケースのウィンドウへ接続する問題も見つかり、起動前に自分の前のテストプロセスを終了するよう修正した。最終的に全11件が成功（1.5分）。計算・Rustの処理は今回変更せず、直前に成功した単体109件・12件の結果を維持する。

- カレンダーの日付選択、週の移動、月・週・一覧の切り替え後に、日別詳細・可処分時間・週間内訳が同じ選択日に一致することを実画面で確認。
- 予定のない日の0問・7問の実績、試験による絞り込み、取消済み記録の非表示、授業だけの期間の表示、未来日の記録ボタンの無効化を確認。統合計画は変更されない。
- `tests/calendar.test.ts` の4件で、月曜開始の週、年越し、月末の切り詰め、うるう年を確認。
- 任意問題数の空欄・負数・小数・残数超過は登録できず、3問を入力してEnterによる事前確定なしで二重クリックしても1件だけ記録される。旧版の入力途中の7問の引き継ぎ、登録後の空欄への復帰、Enter1回での3問追加も確認。
- 保存通信だけをテスト内で待機させ、通常のウィンドウ終了要求が保存完了まで待つことを確認。終了を繰り返し要求しても待機を飛ばさず、保存失敗時は画面を閉じずに保存済み内容へ戻る。正常時は保存後に実プロセスが終了し、再起動後に最後の入力が残る。
- 保存の待機・失敗は実画面テスト内の通信制御で再現し、製品にテスト用の保存機能は追加していない。物理ディスク障害・停電を起こす試験ではない。

この検証で、任意問題数の入力後に「記録する」が無効のままになる不具合を再現して修正した。画面画像は `test-results/calendar-consistency.png` と `test-results/close-save.png`。

## バックアップ・独立したチュートリアル・接続エラー

- TypeScriptの5件で、共通JSON Schemaとの一致、空欄を含む下書きの保持、不正形式・版・画面を壊す型の拒否、配色と初期化前の控えの保持を確認。
- Rustの追加4件で、日本語ファイル名への書き出しと読込、既存ファイルの原子的な置換、不正入力時の保護、形式・日付・参照の検証、復元の原子性・二重要求・古いリビジョンの拒否・再起動後の控え・取消、初回復元から初期状態へ戻す操作を確認。
- 実画面で保存取消、バックアップの実ファイル出力、壊れたJSONと残数超過の拒否、内容確認と取消、確認前の実行無効化を検証。復元・再起動・復元取消後に設定・0問を含む実績・固定・計画履歴・未承認案・下書き・配色の全状態が一致することを確認。
- チュートリアルの6項目を直接選択でき、前後の項目へ進めること、先頭・末尾の移動制限、9個の遷移ボタンの行き先を実画面で確認。入力途中の試験名を残したまま各画面を閲覧し、設定・下書き・実績が変わらず、再読み込み後も一致することを確認。
- 初期設定の一般説明と折り畳み説明欄を撤去したことを確認。警告や入力ラベルは残す。画面画像 `test-results/tutorial.png`、`setup-concise.png`、`backup-preview.png` を確認した。
- 更新途中の実画面テストでは、ViteがRustビルド出力の実行ファイルを監視し、Windowsのファイルロックによる `EBUSY` で停止した。これにより `ERR_CONNECTION_REFUSED` が発生した。`src-tauri`、`release`、テスト出力を監視から除外して再起動した。
- 修正後は実画面テスト7件とreleaseビルドを同時実行して双方が成功し、完了後も開発サーバーのHTTP 200応答を確認した。配布版は別途、同梱画面での起動を確認した。

## 起動時のエラーと再試行

追加のRustテスト3件で、保存フォルダーが使えない場合の再試行、保存失敗後に接続を開き直しても確定済みデータが残ること、壊れたDBを繰り返し開いても元のバイト列が変わらないこと、新しいSQLiteスキーマ版を変更せず拒否することを確認した。

追加の実機E2Eでは、テスト用DBの位置に空のフォルダーを置いて接続エラーを発生させた。アプリが終了せず、エラー詳細に保存先が表示され、再試行の連打でも処理が競合しないことを確認。障害を除くとアプリを閉じずに通常画面へ復帰した。実際に回答を保存した後、読み込みコマンドだけ一度失敗させて、空のデータで上書きせず再試行・再起動で同じ回答を保持した。別のテスト用ファイルではSQLite以外のバイト列を用意し、失敗・再試行後も元のバイト列を維持した。画面画像は `test-results/startup-retry.png`。

最初の一括実行は7件成功・1件が画面接続に失敗。通常のアプリが同時に動作しており、WebView2の共通データ領域による競合が疑われた。[PlaywrightのWebView2公式手順](https://playwright.dev/docs/webview2#writing-and-running-tests)に沿って、テストの画面データを起動ごとに分離し、起動失敗時のプロセス出力を取得するようにした。SQLiteの保存先は従来どおり別のテスト専用領域で維持する。修正後は通常アプリを終了せず、8件すべてが成功（57.3秒）。配布版の起動確認も画面データを分離して成功した。

## v0.2.0：対話式の追加・承認の区別・週の表示・更新パッケージ

試験の名前から登録確認まで、教材の対象試験・問題数・周回ごとの初期完了・時間・順序までを実画面で操作した。途中の空欄を保存し、プロセスを再起動して再開できること、Enter1回で検証と次へ進むことを確認した。登録ボタンを同じタイミングで2回呼んでも試験・教材が1件だけ追加され、別の初期設定下書きは作られない。

登録前は設定に追加されず、登録後も計画・既存の0問記録・学習枠・連続時間・余裕率が変わらないことを確認。計画案に追加した試験・教材名を表示し、破棄しても登録内容と元計画が残る。承認で新教材の学習予定が追加され、固定セッションは時刻・件数を保ち、元計画を履歴へ残す。承認後の完了メッセージとカレンダーへの移動、再起動後の設定・計画・実績一致も確認した。

週の表示は空き枠と学習可能量の2項目だけになり、余裕率の設定値が維持されることを確認。空の設定で教材追加を始めると試験登録へ案内し、学習可能枠が不足する場合は、その入力ボタンを表示する。追加後の画面でWCAG A/AAの自動検査も実施した。

バックアップの新しい追加下書きの保持、不完全な型や対象外の質問位置の拒否を1件追加した。更新用ZIPには版とSHA-256、実行ファイル、旧exeの控えを残す更新処理、手順書を含める。

最終チェック：TypeScript118件・Rust13件成功、型検査と本番ビルド成功。実機E2Eは、最初の14件一括で全件成功。未設定からの追加テストを加えた最終15件一括では14件成功、既存の表でEndキーによる末尾移動が1件タイムアウトした。同テストはコードを変更せず単独再実行で成功（15.8秒）。ビルドと同時の負荷が原因かは特定できていない。15シナリオの成功を確認したが、全15件が一度の実行で成功したという結果ではない。

更新処理は0.1.0のexeのコピーを日本語・空白入りの場所に用意して検証した。ロック中の拒否と対象の維持、パッケージのハッシュ不一致での拒否、0.2.0への置換、旧exeのハッシュ一致する控え、二度目の実行で追加置換しないこと、隣接する保存ファイルの不変を確認。通常のSQLiteデータには更新スクリプトから触れない。更新テストでは対象パスを引数指定しており、Windowsのファイル選択ダイアログ自体は自動操作していない。

配布版は同梱画面 `http://tauri.localhost/`、既存SQLiteの読み込み、正常終了を確認。v0.2.0 exeは2026-09-20 22:04:49更新、17,158,144バイト、SHA-256 `212F97F55970B012EB65D63F3C9786486E7ED0F4948DA118980FECE29FE7A0BE`。`release/StudyPlan.exe`、`release/StudyPlan-20260920.exe`、更新パッケージのexeはすべて同一ハッシュ。対象の配布exeが利用中ではないことを確認して更新した。

`release/StudyPlan-update-0.2.0.zip` のSHA-256は `5D7A1E91E8065570AA35772B263B18DA24DB3C7E27CB734B1F0E31A1C1D058D1`。ZIP内の5ファイル（exe、更新スクリプト、起動用cmd、manifest、手順書）を確認した。これは差分パッチ・自動更新ではなく、exe全体を置換するローカル更新パッケージである。画像は `.test-data/validation-v0.2.0/addition-saved.png` と `addition-approved.png` に保管した。

## v0.2.1：教材の所要時間によるまとまり

TypeScriptは133件、Rustは13件が成功。新しい15件では、60分に短答20問・論文2問を通常配置すること、1〜4問でも時間が十分なら例外理由を付けないこと、23分に3分の問題を7問置けること、目安時間によるまとまりの変化、長すぎる1問は未配置になることを確認した。前の枠に延長できない端数は次の十分な枠へまとめ、整数の総数と所要時間の一致、共有枠・順序・連続上限・余裕率を維持する。周回ごとの時間、旧設定の初期値補完、旧案の承認拒否、不正な下限・目安の拒否も含む。

以前の「最後の30問が6分なら見直し込み10分へ水増し」というテストは、6分の完了例外として保持する期待へ変更した。少ない問題数だけで例外理由を求めていた検査も時間基準に置き換えた。例外処理より先に、全期間で通常の長さの配置を試す。

実機E2Eは全16件が一括成功（1.6分）。既存の初期設定・対話式再計画・追加・記録・承認・バックアップ・週間レポート・ICS・画面の読みやすさを含む。追加シナリオでは、旧案の承認停止と時間基準の案への更新、旧計画の論文2問60分に短時間警告を出さないこと、下限15分・目安60分の保存、論文2問60分の新規配置、固定予定と0問実績の保持、再起動後の一致を確認した。画像はローカルの `test-results/time-based-session.png`。

Git管理の対象はソース・設定・ロックファイル・テスト・仕様書・ビルド用アイコン・更新用スクリプト。保存データ、バックアップ、環境変数ファイル、実行ファイル、ZIP、旧版の控え、ログ、依存パッケージ、ビルド出力、テスト出力を除外した。代表的なパスが除外されることを `git check-ignore` で確認した。

配布用TauriビルドとネイティブSQLite読込・通常終了の起動確認が成功。v0.2.1 exeは2026-09-20 23:02:46、17,164,288バイト、SHA-256 `E829A901E1118D54560FB6AD9344EF19B98753FB412E6F423F46D59329CDCEEC`。更新ZIPは `release/StudyPlan-update-0.2.1.zip`、SHA-256 `8E7B0222CED5FD0F94F55F9DC2A96772027D504E66F1D0B99B8D77BE376626B1`。

v0.2.0のexeのコピーを使い、v0.2.1への更新・旧exeの保管・再実行時の更新済み判定・ロックやハッシュ不一致での拒否と元ファイル保持を確認した。SQLiteの保存先とスキーマは変えず、実績を更新処理で変更しない。更新スクリプトはテスト内で対象パスを引数指定しており、ファイル選択ダイアログ自体は自動操作していない。数学的な最適配置を保証するものではない。

## v0.2.2：未登録期間と長期計画の見直し

学習枠が2月まで、目標日が6月のケースを合成データで再現。従来の計算は3月以降を割当可能時間0として全課題を2月までへ配置するため、算術エラーではなく未設定条件を明示しない設計上の問題だった。未登録期間と影響を表示し、対話で時間枠・周回数・目標日を見直す導線を追加した。

`pnpm test`：146件成功。うち今回の追加13件で、期間の重複・包含・隣接、曜日の休み、授業との区別、複数試験、復習期間、過去・不完全入力、2月への集中と後続期間登録後の配分、下書き・承認時の保持、長期警告、周回数削減時の完了数・0問記録・取消履歴・開始済み予定・固定予定の保護を確認。

`cargo test --manifest-path src-tauri/Cargo.toml --locked`：13件成功。型検査・Webビルドとv0.2.2のTauri配布ビルドも成功。保存データとSQLiteスキーマは変更していない。

新規の実機シナリオは、カレンダーの警告 → 周回数の対話修正 → 不足期間の学習枠の追加 → 案作成 → 承認 → 再起動を実際のWebView2と隔離SQLiteで確認。承認前に設定・計画が変わらず、承認後も固定予定と0問記録を維持し、従来の期間より後へ予定が配分されることを検証した。

未登録期間を自動的に埋めたり、周回数を自動で減らしたりはしない。実際の春休みの曜日・時刻は利用者の入力が必要。長期警告は90日以上の残り計画期間を製品上の基準とする。自動配置は従来のヒューリスティックを維持し、最適解を保証しない。

全17実機シナリオを実行し、16件は初回成功。既存の3テーマ・表キーボード操作の検査でEndキー後のスクロール待機が一度タイムアウトしたが、ビルド終了後の同検査の単独再実行は成功した（3テーマのaxe検査を含む）。今回追加したシナリオは単独・全体の両実行で成功した。

配布版は開発サーバーに依存せず起動し、ネイティブSQLite読込と通常終了を確認。v0.2.2 exeは2026-09-20 23:41:56、17,165,312バイト、SHA-256 `541E7EC8EE7A7E361DCF0AFCE104B69C9DB3121E324690713A89211CF517C6E4`。更新ZIPは `release/StudyPlan-update-0.2.2.zip`、SHA-256 `684CDDA43D0C5C271FB4F69040D1D9230A20332CD50D635A7BB86B67755CC472`。

v0.2.1のexeのコピーから更新テストを行い、ロック中・ハッシュ不一致の拒否、正常な差し替え、旧exeの控え、再実行時の更新済み判定、データファイル保持を確認。利用中のexeを強制終了・差し替えせず、更新パッケージとして提供する。ファイル選択ダイアログ自体の操作は未自動化。

## v0.2.3：承認操作を上部へ移動・装飾メッセージを削除

再計画の承認・破棄ボタンと承認できない理由を上部へ移した。未配置の課題がある場合は、承認対象に含まれない問題数を操作の近くにも表示する。ホームの大きなメッセージ枠・イラスト・励ましの標語、共通の英語装飾見出し、サイドバーの標語を削除。進捗記録・初期設定・試験追加・教材追加の操作を残し、ホームをコンパクトにした。計画・進捗・SQLiteの処理は変更していない。

型検査・Webビルド成功。実機17シナリオを確認し、3配色で上部の承認ボタンがスクロールせず見えること、ホームのメッセージ枠がないこと、競合・未報告の承認禁止と確認後の復帰、初期設定から記録・承認・再起動までを検証。16件は一括実行で成功し、残る1件は見出しを短くしたことによるテストの部分一致（「計画案」が2つの見出しに一致）を完全一致へ修正して再実行成功。既存の3配色のaxe検査と900px幅の確認も成功。

今回は画面変更のため、新しい計算の単体テストは追加せず、関連する既存の実機テストを更新して検証した。テスト用SQLiteと利用中のデータは分離している。

v0.2.3のTauri配布ビルドと単独起動確認が成功（開発サーバー不使用、ネイティブSQLite読込、通常終了）。exeは2026-09-20 23:52:02、17,164,800バイト、SHA-256 `7B4F685A79754721CA546C85E85C3AFFE521D31EAFCB2F681A3A226831A9567A`。更新ZIPは `release/StudyPlan-update-0.2.3.zip`、SHA-256 `FFBAAF24DA7E750A543B6E44048503E4CB3DD0D1A2AD6042F2F0930F7EB50E33`。

v0.2.2のexeのコピーから、正常更新、ロック・破損の拒否、旧exeの保管、更新済み判定、データ保持を確認。利用中のexeは差し替えず、更新パッケージを提供する。ファイル選択ダイアログ自体は自動操作していない。

## v0.2.4：アプリ名の統一（2026-09-21）

利用者によるサイドバーの副題削除を保持し、HTMLタイトル・Tauriウィンドウ名・READMEに残っていた副題も削除した。未使用の副題用CSSを削除し、追跡対象のソース・文書に旧副題や以前の装飾標語が残っていないことを検索で確認した。

型検査・Webビルド・Tauri配布ビルドが成功。配布exeを起動し、HTMLタイトル・ネイティブウィンドウタイトル・サイドバーがすべてStudyPlanで、副題のsmall要素がないことを自動確認。開発サーバー不使用、SQLite読込、初期設定画面への移動、通常終了も成功した。Windowsの実行ファイル情報は製品名・ファイル説明ともStudyPlan、ファイル版・製品版とも0.2.4。保存先identifierは変更していない。今回は表記変更のため、計算の単体テストや全実機シナリオは再実行していない。

exeは2026-09-21 00:02:42、17,164,800バイト、SHA-256 `849D06AC338D5CCD4413E8BD53DB8B6D6FF66B702B280D6562EE81AE0FD735F7`。更新ZIPは `release/StudyPlan-update-0.2.4.zip`、SHA-256 `489D0FEE2AD69F3299E8F64CA86C67DECD1AB825276A96F60BC9951903BE65A6`。

v0.2.3のexeのコピーから、正常更新、ロック・破損の拒否、旧exeの保管、更新済み判定、データ保持を確認。利用中のexeは差し替えていない。更新用ファイル選択ダイアログ自体は自動操作していない。

## v0.2.5：進捗円グラフ・16色・ライト／ダーク／システム

TypeScriptの単体テスト147件とRust13件が成功。表示モードの旧データ互換性、3種類の保存値、不正値の拒否、初期化後の保持、バックアップ形式のUI・保存側一致を含む。

円グラフの実機シナリオが成功。複数教材・複数周回・初期完了数・取消済み記録を含む全体集計、教材と周回の切替、3問の追加・7問への訂正・取消・残りすべて・0問報告、0%と100%、空データ、再起動後の数値、900px幅の表示を確認した。

3配色×ライト／ダークの計6通りで、ホーム・カレンダー・進捗・再計画・履歴・週間レポートと計画表末尾のaxe検査が成功。OSの配色をWebView2で模擬し、システム設定の動的追従と、明示したライト・ダークがOS設定に左右されないことを確認。再起動後のシステム設定保持と再追従も成功。Windowsの設定画面自体は操作していない。

色の編集で16候補と追加色の保存・カレンダーの色変更を確認。ダーク設定を含むバックアップの保存・復元・再起動・復元取消の実機テストも成功した。初回の配色検査では既存のEndキースクロール検査が2巡目で時間切れになったため、キー操作の検査は1回にし、残りの配色では表末尾へ直接移動して配色を検証する形へ整理し、再実行成功。全実機シナリオの一括再実行ではなく、今回の変更に関係するシナリオを実行した。

初期設定 → SQLite保存 → 計画生成 → 記録 → 再計画 → 再起動の実機シナリオも成功。対話式の16色と追加したラベンダーの選択を含む。最初の実行は削除済みの本のイラストを待つ旧テストが時間切れとなり、円グラフの表示と3配色への追従を確認する内容に更新して再実行成功した。

型検査・Webビルド・Tauri配布ビルドが成功。配布版で開発サーバー不使用の起動、SQLite読込、円グラフの領域と表示モード3候補、通常終了を確認した。exeは2026-09-21 00:40:39、17,166,336バイト、SHA-256 `511047D0AFE6E106C23E1874316C140A1A4AF1DBA4BD60394624E0970F7B22DC`。更新ZIPは `release/StudyPlan-update-0.2.5.zip`、SHA-256 `9EA5083DD2FD51CABB22684A14EC3F298E8FB27B007D2FFC2B4583C231B01D86`。

v0.2.4のexeのコピーを使い、正常更新、ロック・破損の拒否、旧exeの保管、更新済み判定、データ保持を確認。利用中のexeは差し替えていない。更新用ファイル選択ダイアログ自体は自動操作していない。

## v0.3.0：今日の進捗・警告管理・折りたたみ・可変幅・通学

TypeScript単体テスト161件が成功。通学9件では授業日の最初／最後、曜日指定、適用期間、0分・無効、日付またぎ、授業・準備・予定の重複除外、共有枠・余裕率・連続上限、承認前の保留、固定予定の保持と競合拒否を確認した。今日の進捗5件では同日・教材・周回の集計、超過の別表示、初期完了数・過去日・取消・復習・承認待ち案の除外、0問と未報告、予定なし、訂正を確認。既存のバックアップテストへ通学・警告・折りたたみの保持も追加した。

Rust単体テスト14件が成功。保存検証に通学の共有スキーマ検証を追加し、不正な曜日・時間を原子的に拒否するテストを追加した。既存の再起動テストも通学・警告・折りたたみを含めて保存内容の一致を確認した。SQLテーブル変更は不要で、既存JSONへ任意項目を追加している。

全19実機シナリオを実行。17件が初回成功、残り2件は開閉ボタン化した警告の旧テキストセレクターと計算版4の固定期待を更新し、再実行成功。既存の初期設定・記録・訂正・取消・固定予定・再計画・授業名・ICS・週間レポート・バックアップ・保存失敗・再起動・3配色×明暗のアクセシビリティを確認した。

追加シナリオでは、警告の開閉・無視・再起動後の非表示・管理画面からの復帰、サイドバー折りたたみによるmainの拡張と再起動後の保持、通学の対話入力（空欄からEnter1回も含む）、承認前後の設定、固定予定・実績保持、授業・通学の非重複を確認。再計画の対話が通学設定を引き継ぎ、曜日指定へ変更した案を承認せず破棄できることも追加検証した。

表示幅1400・900・640・480pxでホーム・カレンダー・進捗・教材・通学・警告管理を確認し、480pxでは時間割・連続時間・試験・初期設定・週間レポート・履歴・バックアップ・チュートリアル・再計画も横方向にはみ出さないことを確認した。最小幅のダーク表示でaxe検査も成功。実際のWebView2へ表示幅を設定しており、WindowsのDPI変更や複数モニター間の移動は未検証。

進捗バーの値21%、700msの遷移、最終描画、prefers-reduced-motionによる0秒への変更を実機確認。円グラフは記録・訂正・取消、今日と周回の切替、再起動後の一致を確認した。動きの抑制はブラウザーのメディア設定を模擬しており、Windowsの設定画面自体は操作していない。

型検査・Webビルド・Tauri配布ビルドが成功。配布版で開発サーバー不使用の起動、ネイティブSQLite読込、今日の進捗と表示モードの表示、通常終了を確認した。Windowsの製品名はStudyPlan、ファイル版・製品版は0.3.0。exeは2026-09-21 01:39:44、17,206,272バイト、SHA-256 `C12B5688E5D2BB8E63C527E3BB8168FCA6772D2A31AF395B182B247271E15C42`。更新ZIPは `release/StudyPlan-update-0.3.0.zip`、SHA-256 `11627255E30D68620596E6392146D5F1116BFA76C8F4DB45ADE3B0ED93BCB618`。パッケージ内exeとビルドしたexeのハッシュは一致した。

v0.2.5のexeのコピーから、正常更新、ロック・破損の拒否、旧exeの保管、更新済み判定、データファイル保持を確認。利用中のexeは差し替えていない。更新用ファイル選択ダイアログ自体は自動操作していない。

## v0.3.1：サイドバー端の矢印による開閉

上部の開閉ボタンを、サイドバー右端の細いレール中央にある◀へ変更。閉じると幅24pxのレールと▶だけを残す。スクロール中も画面中央から操作でき、開閉状態のSQLite保存を維持する。

型検査・Webビルド・Cargo check・Tauri配布ビルドが成功。関連する実機シナリオ1件で矢印の向き、レールとmainの境界位置、画面の高さへの追従、全メニューの非表示、再起動後の保持、Enterでの再表示、480〜1400pxの各画面、最小幅のダーク表示のaxe検査を確認。初回は再計画の質問とサイドバーの同名「通学時間」をテストが取り違えたため、操作対象を再計画の領域に限定して再実行成功。開閉のスクリーンショットも確認した。計算・DB形式は変更しておらず、単体テストや全実機シナリオの一括再実行は行っていない。

配布版の開発サーバーなしの起動・SQLite読込・通常終了が成功。v0.3.0のコピーから正常更新、ロック・破損の拒否、旧exeの控え、再実行の更新済み判定、データファイル保持を確認。exeは2026-09-21 01:54:30、17,205,760バイト、Windows製品版0.3.1。利用中のexeは差し替えず、`release/StudyPlan-update-0.3.1.zip`として提供する。更新用ファイル選択ダイアログ自体は自動操作していない。

SHA-256：exe `E8D233FD1D7EF49E468F6B9F9F9FD8E4A648D1498A37A16535DD9D9237BABFC5`、更新ZIP `08866B5CF25B6778339A355534262209E9FB3602B439258CBF2EC7FE0ADC4F72`。

## v0.3.2：サイドバーと開閉ボタンの境界線を削除

サイドバーの右辺と開閉レールの左辺を線なしにし、同じ背景色で連続する表示へ変更した。開閉処理・状態保存・キーボードフォーカス表示は維持。型検査・Webビルド・Tauri配布ビルドが成功し、配布版で両辺の線幅0pxと背景色の一致を検査、スクリーンショットも確認した。開発サーバーなしの起動・SQLite読込・通常終了と、v0.3.1からの更新テスト（ロック・破損拒否、控え、再実行、データ保持）が成功。CSSのみの変更のため単体テスト・全実機シナリオは再実行していない。

SHA-256：exe `433F80EAC5467904597DDAA9F0330CDAD335FC3FA8C6A53DE3AE07F6F9940244`、`release/StudyPlan-update-0.3.2.zip` `68890F2810E4A0C3549F07BFAB888AFA24C349D82B3752FF5A1DF37D3E875ED1`。

## v0.3.3：復路の表示・簡略内訳・授業間10分以下の除外

往路08:10〜09:00、授業09:00〜10:40／10:50〜12:30、復路12:30〜13:20、昼食12:30〜13:30のケースで、復路50分が食事側の集計に隠れる表示を再現した。往復合計100分と両方向の時刻を表示し、重複50分を独立区分にして24時間合計と二重差し引き禁止を維持した。

TypeScript単体テスト172件が成功。食事との全重複・部分重複・重複なし、往復合計、可処分時間、24時間合計、学習・休憩・余裕の表示集約、授業間5／10／11／60分、重複授業・準備時間・予定の和集合、移動時間に重なる固定予定の保持と修正先を検証した。授業間10分を以前は学習に使うとした既存テスト2件は、新しい仕様に合わせて期待値と説明を更新した。

関連する実機2シナリオが成功。ホームの往復100分・復路時刻・食事との重複表示、09:00〜12:30の授業・移動の集約、13:30〜21:30の学習枠の集約、細かな休憩行の省略を確認。480pxの明暗両表示で横はみ出し・axe検査を行い、スクリーンショットも確認した。設定・記録は表示操作で変化しない。既存の通学の対話・承認・固定予定・再起動・各画面幅のシナリオも成功。初回の追加シナリオは「1時間 40分」の空白を含まないテスト期待値を修正して再実行した。全実機シナリオやRustの保存テストは今回は再実行していない（SQL・保存形式に変更なし）。

型検査・Webビルド・Tauri配布ビルドが成功。配布版の開発サーバーなしの起動、SQLite読込、通常終了を確認。v0.3.2のコピーから、正常更新・ロックと破損の拒否・旧exeの控え・再実行の更新済み判定・データファイル保持を確認した。利用中のexeは差し替えていない。

SHA-256：exe `8FBCBBE4FEAC742059120C8BC64C6966B7F4F4EFBACDB75CF969B4FBD5F6CC5C`、`release/StudyPlan-update-0.3.3.zip` `B2D4608E685F03503E2DD77A72136CC2972808DB63317B93B38CDCF7DE5A62BE`。

## v0.4.0：余裕率を週全体の上限へ変更

日別の余裕率による時間枠の切り詰めを削除し、日別のslotsは休憩を除いたblocksと同じにした。月〜日の容量を合計し、余裕率を適用した上限をすべての試験・教材・復習・固定予定で共有する。通常配置、復習の先行確保と追加、期限例外、端数統合の各経路を検証。ホームの24時間内訳には余裕の区分がなく、週の条件表では学習可能30時間・上限24時間・予定済み24時間・未割当6時間を確認できる。

TypeScript単体テスト183件が成功。追加した週容量11件は、30時間→24時間、一日の6時間を全て利用、日割り上限を超える期限前の配置、余裕率変更で日別枠・休憩不変、日曜／月曜境界と端数統合、複数試験・復習の共有、週途中の保持予定、固定超過の競合、承認時の再検証、過去の超過を改変せず追加を止めること、承認・取消で記録維持、週一度の丸めを含む。既存の日次上限の期待値は週の検証へ更新し、既存の配置パターン検査にも週合計の上限を追加した。

全21実機シナリオを実行し20件が初回成功。以前の授業間10分を学習に使う前提で重複2件としていた1件を、現在の仕様の3件へ修正して再実行成功。追加した週上限シナリオも表示調整後に再実行成功した。旧計画が承認前に変わらないこと、新方式の承認、再起動後の計画保存、計画を戻しても0問報告を保持することを確認。配色・アクセシビリティ・初期設定・通学・進捗・復習・固定・対話式再計画・バックアップ・保存失敗・ICS・レポートの既存シナリオも含む。

SQL・保存形式は変更していないためRustの保存単体テストは再実行していない。既存の余裕率は引き継ぎ、計算方式7の案を承認するまで保存済み計画を更新しない。週途中の容量は月〜日全体の登録済み枠を基準とし、過去の消費には実測でなく保持する予定時間を使用する。最適な配置を数学的に保証するものではない。

型検査・Webビルド・Tauri配布ビルドが成功。配布版で開発サーバーなしの起動、SQLite読込、通常終了を確認。v0.3.3のコピーから正常更新、ロック・破損の拒否、旧exeの控え、再実行時の更新済み判定、データファイル保持を確認した。利用中のexeは差し替えていない。更新用ファイル選択ダイアログ自体は自動操作していない。

SHA-256：exe `FFAC4DCFD9D5A0F70BE81C20F7BF26E1232C5548D7709A70201D8F37D0FF5250`、`release/StudyPlan-update-0.4.0.zip` `FA7AEAADBD247EA58F98C6D69299C6D8E4BC16F2E448F6CD983C4A6149CA253F`。

## v0.4.1：ホームの導線とカレンダー密度

TypeScript単体テスト183件が成功。表示密度をバックアップの検証対象へ追加し、月・週・一覧の選択を保存・読込する既存テストを更新した。生成JSON Schemaと画面側のスキーマ一致を確認。計画の計算方式は7のままで変更していない。

追加した実機シナリオでは、ホームのスケジュールボタンが記録の左側にあることと両ボタンの移動先、3表示×3密度、初期値、試験の絞り込みと色、授業の保持、0問報告、480pxでの横はみ出しなしを確認。選択は表示ごとに独立してSQLiteへ保存され、アプリ再起動後も復元された。計画・実績の値が変わらないことを確認。ライト・ダークのaxe検査と月表示のスクリーンショット確認も実施した。

関連する既存の実機7シナリオも成功（3テーマ・明暗の読みやすさ、ICS、固定予定の修正、初期設定から記録と再計画、今日の予定と初期化・復元、月移動と選択日・週内訳、バックアップ復元と取消）。今回の実機検証は新規を含む8シナリオで、全22シナリオの再実行ではない。

Rustの保存・バックアップ・書き出し等の14テストが成功。型検査とWebビルドも成功した。

Tauri配布ビルドと、配布版の開発サーバーなしの起動・SQLite読込・通常終了が成功。v0.4.0のコピーから正常更新、ロック・破損の拒否、旧exeの控え、再実行の更新済み判定、データファイル保持を確認した。利用中のexeは差し替えていない。更新用のファイル選択ダイアログ自体は自動操作していない。

SHA-256：exe `4ED707F50A48833BCA120720448D7A8C124CCBA7904B7BFC1D5BE1B728D5ACFA`、`release/StudyPlan-update-0.4.1.zip` `3A34340DF3E4AB7D264E42885972DF6BDDE33E8EF41508089168840A33AE1DF7`。

## v0.4.2：代表的な利用データによる整合性監査

設計・検証対象・発見事項・変更しなかった事項は `REAL_USE_AUDIT.md` に整理した。架空の2試験・2教材×2周、3分/問と30〜40分/問、授業期間と休暇、往復通学、食事、重複する定期予定、終日予定の8週間を共通fixtureとし、計算・画面・保存・書き出しの検証へ使用した。

不具合を再現テストで確認してから修正：記録時に候補条件が失われる問題、固定枠の所要時間不足、固定による教材・周回順序の逆転。実画面で見つかった同教材の隣接カード分割も、同じ集中枠に入る新規予定に限り統合した。旧データの固定・過去予定や実績は勝手に変更しない。新しい計算方式は8。

最終のTypeScript単体テスト191件が成功。追加8件では、代表データ全体の制約、候補条件の記録・訂正・取消への引継ぎ、失敗時の下書きと実績保持、固定時間不足と承認時の再検査、固定順序の逆転、完了済みの前周回、短くなった推定時間、開始済み予定の保持、隣接セッションの統合と固定不変を確認。Rust単体テスト14件も成功。

初回の全実機検証は既存22件が成功し、新規2件は期待する保存メッセージと見出しの選択方法を修正。次の全24件は成功した。その後、画面確認で見つかった隣接カードの統合を追加して最終検証を行う。

最終コードで `pnpm lint`（警告0件）、`pnpm typecheck` が成功。全24実機シナリオも3分12秒で成功した。代表データの今日の予定のスクリーンショットを再確認し、論文の45分×2枚が13:30〜15:00の2問・90分の1枚に統合されたことを確認。両試験、授業、日別実績、往復通学、食事との重複表示を保持している。

`pnpm desktop:build` が成功。配布版の開発サーバーなしの起動・SQLite読込・通常終了、v0.4.1のコピーからの更新テスト（ロック・破損拒否、原子的な差し替え、旧exeの控え、再実行、データ保持）が成功。ユーザーが利用中のexeは差し替えていない。

SHA-256：exe `EA2C370761FD57BE896D65030C5AD10FAD3BE0AB839C8AB6613EC41F55A7126E`、`release/StudyPlan-update-0.4.2.zip` `1FED0A7306BB37B1E8F126CC09BF5DD84E7F9157792421E0874B7F161791D254`。

## v0.4.3：責任分離・食事と通学・一括スキップ（2026-09-21）

指定された `app/`、`hooks/`、`components/guided-setup/`、`components/setup/`、`domain/planner/` に実処理を分割した。元の巨大ファイルを残す転送用の分割ではない。保存の実装は専用フック、次の質問・周回反復・戻る・回答登録は純粋な遷移関数、計画生成・提案・承認は外部から日時とID接頭辞を受け取る計算になっている。

検証結果：

- ESLint（計算層の依存制限を含む）と型検査：成功。
- TypeScript：17ファイル・199件成功。日時・ID取得を例外に差し替えても提案と承認が実行できること、同じ入力で同じ計画になること、入力を変更しないことを検証した。
- Rust：14件成功。SQLiteの再起動保存、競合、二重送信、破損保護、復旧・復元を含む。
- Tauri/WebView2：全25シナリオ成功。さらに通学・食事の独立編集へスキップを揃えた後、関連4シナリオを再実行して成功。
- 一括スキップ：空欄を拒否、数値入力を1クリックで確定して確認へ移動、未変更設定を保持、再起動後も確認段階を保持、承認まで元の設定・計画・実績を維持。
- 初期設定の再編集：試験名の変更だけを保存して確認へ移動。通学の単独編集は登録確認へ進み、食事は現在の質問の変更だけを保存して既存の他の食事を保つ。
- 食事：復路12:30〜13:20と重なる60分の昼食は13:20〜14:20へ移動。往復100分・食事全体の時間・24時間の合計を保ち、学習可能枠から除く。日またぎ、通学のない日、食事同士の調整も検証。

旧固定予定が調整後の食事と重なる場合は競合として残す。承認済み計画・過去・実績の自動書換えは行わず、計算方式9の新案を承認して反映する。SQLite形式の変更はない。外部の通学経路や実際の学習量を検証するものではない。

最終配布ビルド・同梱フロントエンドとSQLiteの読込・通常終了に成功。v0.4.2からの更新では、使用中ファイル・破損ファイルの拒否、原子的置換、旧exeの控え、再実行、データ維持の確認に成功。依存パッケージのコメント注釈に関するビルド警告は既存どおりで、ビルド失敗はない。

配布物は `release/StudyPlan-0.4.3.exe` と `release/StudyPlan-update-0.4.3.zip`。SHA-256：exe `57C6C7F2CCD66B70F9270B2CAE9A4820062377A9252F376B047BDA1F9A5C3195`、ZIP `067B6D86927B2D0B6D2025B5BB0688A030024A2866E0BC47703D4ECE0745AC51`。

## v0.4.4：往復の出発時刻・一括スキップ削除・開閉幅（2026-09-21）

- TypeScript単体テスト：17ファイル・197件成功。指定した出発時刻を授業の変更で動かさないこと、食事を自動移動しないこと、日またぎの重複、旧設定の再確認、候補設定を承認する際の再検証を含む。
- 型検査・ESLint：成功。食事質問の最終調整後も再実行した。
- Rust：14件成功。SQLite保存、再起動、競合、バックアップと復元、破損データ保護を確認。
- 実機：全25シナリオ成功。その後、食事の質問を食事ごとに検証する調整を行い、新規1件と関連2件の計3件を再実行して成功。検証した異なるシナリオは26件。
- 通学：旧授業日方式では出発時刻を空欄から確認。往路08:10・50分、復路12:30・50分は昼食12:30〜13:30と重なるため拒否。修正ボタンから復路13:30へ変更すると保存でき、再起動後も復元された。食事時刻は変化しない。
- 食事：昼食の重複があっても朝食の質問を通過し、昼食の長さを確認する段階で重複を表示。前の質問へ戻って昼食を13:30へ修正し、夕食の質問へ進めた。
- 一括スキップが初期設定・再計画・通学から消えたこと、サイドバー開閉レール12px、折りたたみ後の表示幅、保存・可変幅・配色を確認。出発時刻の画面のスクリーンショットも確認した。

旧承認済み計画・実績は維持し、新しい出発時刻の確認と計算方式10の案の承認で反映する。通学の適用日ごとに異なる出発時刻を指定する機能は今回追加していない。授業日方式でも往路・復路それぞれ一つの出発時刻を使用する。

最終配布ビルド、開発サーバーを使わない起動、同梱画面とSQLiteの読込、通常終了に成功。v0.4.3からの更新テストでは、使用中ファイル・破損ファイルの拒否、原子的な差し替え、旧exeの控え、再実行、データ保持を確認した。利用中のユーザーのexeは差し替えていない。更新用ファイル選択ダイアログ自体は自動操作していない。

SHA-256：exe 8495B01CEBD3560DA11DD05C8103F756789DA94802A45AD3FECFE693B81BA3A7、release/StudyPlan-update-0.4.4.zip CD47C597C1B5FE68393EC37835CB900FD1348BB7DB55B9F00C40EA47E9BDB24C。

## v0.4.5：1日の可処分時間の読みやすさとアクセシビリティ（2026-09-21）

確認対象は DailyTime の表示と専用CSS。既存の dailyTime / planner / 保存スキーマ / 設定処理には変更を加えていない。ソース差分と、表示操作後の settings・plan・records および dailyTime の計算結果が同一であることを検証する。

検証基準は [WCAG 2.2 Level AA](https://www.w3.org/WAI/WCAG22/quickref/)。自動検査だけでアプリ全体の準拠を断定しない。

| 項目                           | 検証内容                                                                                                                                                                                         |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1.3.1 / 2.4.6 / 4.1.2          | ページのh1に対するh2、固有IDとaria-labelledby、dlのdt/dd、captionとscope付きの区間表、ネイティブdetails/summaryを使用。空状態・複数表示・警告表示も単体テスト。                                  |
| 1.4.1                          | 分類・合計・全区間・往復の通学をテキストで提供。グレースケール画像を確認。補助バーはaria-hidden、titleへの依存なし。                                                                             |
| 1.4.3 / 1.4.11                 | mint/sky/lime × light/dark/system-light/system-darkの12条件で対象部分のaxe違反0。文字と実背景、フォーカス枠と背景の計算比を別途測定。最低比はいずれもlime-lightの5.65:1。                        |
| 1.4.10 / 1.4.12                | 480px（サイドバー展開）・320px（折りたたみ）で検証。全テキストを元の200%へ拡大し、行高1.5、字間0.12em、単語間0.16em、段落後2emを同時に適用。対象要素とページの横はみ出し、欠落・クリップを確認。 |
| 2.1.1 / 2.4.7 / 2.4.11 / 2.5.8 | Tabでsummaryへ移動し、EnterとSpaceで開閉。フォーカスの枠・画面内表示・前面要素で覆われていないこと・24×24px以上を検査。44px以上の操作高さを確保。                                                |
| 集計の維持                     | 主指標＝capacity.focus、補助＝capacity.free、全7分類の合計、各区間の長さを既存計算結果と照合。バーは実時間比率を維持し、CSSの小数丸めのみ許容。                                                  |

スクリーンショットで、主指標が最初に認識できること、凡例のラベルと値の近接、詳細が標準で閉じること、重複・未確認の警告が詳細の外にあることを確認。拡大時の時刻途中改行を避けるため表の列幅を内容に合わせ、バーの目盛は両端に整理した。

実際のスクリーンリーダーの音声出力、すべてのOS拡大設定、外部端末では未検証。今回のテストはWindowsのTauri/WebView2で対象部分を検証したもので、StudyPlan全画面のWCAG適合認証ではない。

結果：型検査・ESLint成功、TypeScript単体テスト18ファイル・200件成功。実機全27シナリオ成功後、時刻の折り返しと目盛を仕上げて対象2シナリオを再実行し成功。最終スクリーンショットで6配色、グレースケール、320px、200%文字拡大＋間隔変更を確認した。axeのincompleteも対象12条件ですべて0件。

配布版v0.4.5のビルド、開発サーバーなしの起動・SQLite読込・通常終了に成功。v0.4.4からの更新テストでロック／破損の拒否、原子的置換、旧exeの控え、再実行、既存データ保持を確認した。使用中のユーザーのexeは変更していない。

SHA-256：exe BC31381957C0DDB0E4FA854FE32D41A943CAC53027B08C8B68BB5AA2B5BD514F、release/StudyPlan-update-0.4.5.zip 408AF1C866E0C2B12358AD942B5A27946EBD0B780B359620189761CDB1C57E11。

## v0.4.6：可処分時間の円グラフ（2026-09-21）

単体テスト200件、型検査、ESLint成功。対象の実機2シナリオが成功。円弧の分量を既存カテゴリ合計と照合し、開始位置が順に連続して合計1440分となること、食事・通学の重複が二重計上されないこと、未設定のみの日は1本の完全な円になることを確認した。

mint/sky/lime × light/dark/system-light/system-darkの12条件で対象部分のaxe違反0件。既存のキーボード開閉・フォーカス・320pxと480px・200%文字拡大と間隔変更・設定/計画/実績の不変性の検査も成功した。スクリーンショットで横並びと縦並び、グレースケール、円グラフとテキストの対応を確認。参考画像の装飾文言は追加していない。

今回の実機再実行は対象2件で、全27件の再実行ではない。スクリーンリーダーの音声出力は未検証。計算・保存スキーマ・設定のソースには変更なし。

配布ビルド・開発サーバーなしの起動とSQLite読込・通常終了に成功。v0.4.5からの更新（ロック・破損拒否、旧exeの控え、再実行、データ保持）を検証した。SHA-256：exe EC0D392DA9314150A128F5255A3C7D1BBB7AD5A20CB36367E4F39F0CC2E32CCA、ZIP 35456B09E68C19A414427A46FA3BFBE2650FFE3BBC025AE14CC1E9FD25ECA55E。

## v0.4.7：通学・食事の統合表示と任意の生活時間（2026-09-21）

TypeScript単体テスト19ファイル・211件、型検査、ESLint、Rust14件成功。睡眠・風呂の日またぎ、空欄・不正時刻・相互重複の拒否、旧バックアップの読込、下書きと確定値の保存・再起動・解除、初期化からの復元を検証した。生活時間を変更しても既存の計画・固定・実績・学習可能量は変わらない。

WindowsのTauri/WebView2で全28シナリオ成功。初期設定から計画・進捗・再計画、バックアップ・復元、保存失敗と復旧、カレンダー、レポート、ICSなど既存機能の回帰テストも含む。

円グラフの通学・食事は一つの円弧と凡例へ統合。重複を含むテストデータでも円弧の合計1440分を維持し、各内訳の分類は保存されることを確認した。各分類名の下線色と円弧色を照合し、全テーマで睡眠の背景との差が学習可能の背景との差より小さいことを検査した。

mint/sky/lime × light/dark/system-light/system-darkの12条件で対象部分のaxe違反0件。下線を加えた文字にはcolor-contrastの自動判定保留があるため、別途計算で文字・実背景とフォーカスの比を検証し、最小5.65:1を確認した。円グラフ・色見本・分類色の下線は補助表現で、内容を隣接テキストから取得できる。スクリーンショットで睡眠の控えめな配色、通学・食事の境界がないこと、凡例のまとまり、320px表示と200%文字拡大・文字間隔変更を確認。キーボードでの詳細開閉と可視フォーカスも成功。スクリーンリーダー音声出力は未検証であり、アプリ全体のWCAG適合を断定するものではない。

配布版ビルド、開発サーバーなしの起動・SQLite読込・通常終了に成功。v0.4.6からの更新テストでロック・破損拒否、旧exeの控え、再実行、既存データ保持を確認した。利用中のユーザーのexeは変更していない。任意設定は毎日共通の時刻で、曜日別の睡眠・風呂設定は今回含めない。

SHA-256：exe B766185597DB99183C4753ADEC4408B68C9781712C73D2F7E0D27C7A32D4A766、ZIP F9D595F9A509D2D58A4A88962CC76F9FB68B0E79B057CB08077AE08CEEA1AFF6。

## v0.4.8：円の中心へ主指標を集約・リリース（2026-09-21）

型検査・ESLintとTypeScript単体テスト211件成功。実機は表示に関連する2シナリオを再実行し成功。直前のv0.4.7で全28シナリオが成功しているが、今回全件の再実行はしていない。計算・保存スキーマ・Rust実装には変更なし。

mint/sky/lime × light/dark/system-light/system-darkの12条件で、学習可能量が円の空白内の中央に収まること、概要の重複表示がないこと、中心のdlが読み上げ対象でSVGのみが補助表現であることを検証。各分類の円弧・合計・警告は維持。axe違反0件、文字とフォーカスのコントラスト、キーボード開閉、320pxと480pxのリフローを確認。200%文字拡大と文字間隔変更で値を円の下へ移し、円弧と重ならず、元に戻すと中心へ戻ることも検証した。スクリーンショットでライト・ダーク・狭い画面の配置を確認した。スクリーンリーダーの音声出力は未検証。

配布版ビルド、同梱画面の起動・SQLite読込・通常終了、v0.4.7からの更新（ロック・破損の拒否、旧exeの控え、再実行、データ保持）が成功。利用中のユーザーのexeは変更していない。配布物はStudyPlan-0.4.8.exe、StudyPlan-update-0.4.8.zip、SHA256SUMS.txt。

SHA-256：exe 53B99F7344EB33EB6F821FC6229395597B1060B5F3FECDD000C42DD3ECC6E899、ZIP 74C99847F042AA21ED46B62630A055DC71CAF40C808BFB1BC90DA9BE85D58AB7。

## v0.4.9：区間名の編集・項目別の修正・日合計（2026-09-21）

型検査・ESLint、TypeScript単体テスト20ファイル227件、Rust14件成功。未設定区間の任意名・区間分割・解除・古い区間への保存拒否・バックアップと復元、項目別の質問遷移、授業の重複を除いた合計・試験別集計を検証した。名前の変更で設定・学習可能量・計画・実績を変更しない。

WindowsのTauri/WebView2で全30シナリオを実行し27件成功。カレンダーの日合計にグループの意味を追加し、一覧の詳細を開くテスト操作を修正した後、失敗した3件を含む関連5件の再実行が成功した。全30件が修正後の一度の実行で成功したという意味ではない。

区間名は空欄の検証、Enterによる保存、取消・初期名への復帰、再起動後の保持を確認。項目別修正は途中再起動後も選択範囲を維持し、完了時に他の項目へ進まない。円グラフは睡眠・風呂を同じ円弧へまとめ、元の分類と1440分の合計を維持した。

mint/sky/lime × light/dark/system-light/system-darkの12条件でDailyTimeを確認。axe、文字・フォーカスのコントラスト、キーボード開閉、320px・480px、200%文字拡大と文字間隔変更を検証した。画面全体の6配色のアクセシビリティ検査も成功。スクリーンショットで区間名の編集、生活時間の統合凡例、カレンダーの日合計を確認。スクリーンリーダー音声出力は未検証。

配布版ビルド、同梱画面の起動・SQLite読込・通常終了、v0.4.8からの更新（ロック・破損の拒否、旧exeの控え、再実行、データ保持）が成功。利用中のユーザーのexeは変更していない。リリース説明は変更点のみとし、検証詳細はこの文書に保存する。

SHA-256：exe 83FAE1178817A22B6EE159D2FE14FA464DB0E7690CD65373B0F31D8DEF97D63E、ZIP 83EE04A48B0F04FE69A8940CCECCB4EE4FF1F6CAEBDE7D9A52A38E607B5C9AE8。

## v0.4.10：名称編集の簡素化とエラーの自動解除（2026-09-22）

型検査・ESLint、TypeScript単体テスト20ファイル228件成功。必須入力エラーを設定保存後に再検証し、解消した項目だけを除外して全解消時に表示を閉じることを確認した。入力途中の下書きではエラーを維持する。

WindowsのTauri/WebView2で対象2シナリオ成功。ペンアイコンの位置・読み上げ名・キーボード操作・可視フォーカス・axe、名称の保存・取消・再起動・解除を確認した。エラー自動解除後も操作ボタンのフォーカス、背景の操作、保存データを維持する。スクリーンリーダー音声出力と実機全シナリオの一括再実行は未検証。

配布版ビルド、同梱画面の起動・SQLite読込・通常終了、v0.4.9からの更新（ロック・破損の拒否、旧exeの控え、再実行、データ保持）が成功。SHA-256：exe F217B7B4941E234A9122CD1024973E8FCEF73D91FCD37DB957EC8EBBFB568BD0、ZIP 065391D199B85DD34D3146F305F4AD7E2F66316FDC450EEA14AC1977F2FA1A52。

## v0.4.11：文字位置・ウィンドウサイズ・公開デモ（2026-09-22）

型検査・ESLint、TypeScript単体テスト22ファイル234件、Rust14件成功。保存サイズがない場合の既定値、片方だけ表示領域を超える場合の寸法別補正、旧バックアップの読込、windowSizeを含むバックアップと全初期化時の引継ぎを検証した。

WindowsのTauri/WebView2で全32シナリオを一度の実行で成功。910×680へ変更し、通常のウィンドウ終了後に同じSQLiteデータで再起動して、内側の論理サイズと保存値がともに910×680へ戻ることを確認した。最大化・最小化は保存対象外。配布用最終ソースでも対象2シナリオを再実行した。

詳細表は、編集アイコンのある未設定区間、アイコンのない学習可能区間、補足の「学習対象外」、通学の「往路」「復路」の文字開始位置を実測し、差が1px未満であることを確認した。スクリーンショットでも同じ縦列に揃い、編集・保存・取消・キーボード操作と狭い表示のaxe検査を維持した。

公開デモはGitHub Pages用の本番ビルドに成功。Microsoft Edgeで、起動、localStorageへの保存、再読込後の保持、バックアップ入口とICS入口の非表示、Markdown書き出しの無効化を確認した。デモの静的ファイル以外にサーバーはなく、デスクトップのSQLiteとは共有しない。

配布版の開発サーバーなし起動・SQLite読込・通常終了、v0.4.10からの更新でロック／破損の拒否、原子的置換、旧exeの控え、再実行、データ保持が成功。SHA-256：exe 55724D8C7861C6A297CA61D729C0364A973FC1C09BD34B5E885AA1E804121F88、ZIP DB00CD6DBFE1232268CF88B71215664F1B8B06B085CE33D6CFA6F1475D91628E。

未実施：複数モニター間の移動、モニターごとに異なるDPI、保存サイズより小さい実モニターを使った起動の手動確認。表示領域への寸法別補正は純粋関数の単体テストまで実施した。実スクリーンリーダーの音声出力も未検証。

## v0.4.12：ウィンドウ保存・復元の修正（2026-09-22）

旧実装で、OSから最大化して通常終了した後、再起動時に最大化が失われることをテストで再現（期待true、実際false）。React側の遅延保存を削除し、Rust側のウィンドウイベントで捕捉、SQLiteの表示設定へ保存、表示前の復元へ変更した。window-state等のプラグインは追加していない。

型検査・ESLint・TypeScript単体231件、Rust17件、配布ビルド成功。Rustでは旧windowSizeの引継ぎ、新テーブルでの再接続後の保持、学習データ・revision・auditを変更しないこと、保存なし、作業領域・最小サイズによる寸法別補正を確認した。旧TypeScriptの寸法補正テスト3件はRustへ移した。

実機の対象5シナリオが成功：通常サイズの再起動、最大化・最小化を含む再起動、保存中の終了待機と保存失敗時の中止、保存と再読込の同時失敗からの復旧、起動時の保存先エラーと壊れたDBの保護。Win32のSetWindowPos・ShowWindow・通常終了メッセージを使用し、WebViewの表示サイズだけの変更で済ませていない。

配布ファイル `release/StudyPlan-0.4.12.exe` でも `scripts/window-release-smoke.mjs` を実行。開発サーバーなしで910×680・820×610へ変更して終了・再起動し、それぞれ同じ内側論理サイズを復元した。最大化して終了した場合と最大化から最小化して終了した場合は最大化を復元し、通常サイズへ戻すと820×610を維持。通常状態から最小化して閉じた場合は通常状態で復元した。各起動で学習データとリビジョンが一致することを確認し、最後に元のウィンドウサイズと状態へ戻した。

配布版の通常起動・SQLite読込・画面遷移・終了も成功。v0.4.11からの更新用ZIPは、ロック中の更新拒否・破損拒否・原子的置換・旧exeの控え・再実行・データ保持を確認した。画面と実測値は `test-results/release-window-restored.png`、`release-window-maximized.png`、`release-window-results.json` に出力。

未実施：複数モニター・異なるDPI間の移動、物理的に小さいモニターへの交換、手で枠をドラッグする操作、全画面のアクセシビリティ再検査、全実機シナリオの再実行。通常終了以外の強制終了・電源断で最後の寸法を保存する保証は対象外。

## v0.4.13：条件変更の再計画導線・前倒し実績・再計画画面（2026-09-22）

`tests/progressReflection.test.ts`を追加し、予定20問に実績30問なら同一教材・同一周回の次回を10問減らすこと、20問に10問の未達後に次回20問へ30問記録しても累計どおりなら先を減らさないことを確認した。別教材・別周回・固定予定を変更しないこと、複数予定への反映、周回完了、配置先なし、訂正・取消・同一ID再送・JSON再読込、再計画へ取込済みの実績、旧計画の開始済み予定、未承認の周回変更、反映例外時の実績保持も検査した。

TypeScript単体22ファイル・241件、型検査、ESLint、Rust17件、本番Webビルドが成功した。バックアップは新しい任意のprogressBaselineを含むJSON Schemaを再生成し、旧データを許容したまま破損・参照・復元テストを通した。SQLiteテーブルは変更していない。

検証専用SQLiteとWebView2データを使うTauri実機35件が成功した。予定超過30問の記録日と全量保存、将来予定10問の減算、再起動、20問への訂正による予定復元を確認した。表示色だけの変更では「計画に未反映」を出さず、周回数2→3の確定では表示して、同じ変更を保持した案へ直接移動した。承認待ちの候補条件を保持したまま進捗を記録し、現在残数で案を作り直すまで承認できないことも確認した。

架空データで、通常の周回変更、条件変更なし、別教材を含む予定量の変化、未配置、固定競合、未報告を表示した。詳細を閉じた標準表示だけで「変更した条件」「計画への主な影響」「対応が必要な問題」「適用操作」と適用可否を確認できた。480px幅、文字間隔・行間隔の拡大、details/summaryのEnter操作、3テーマ×明暗のaxe検査、表のキーボードスクロールに成功した。画面は`test-results/replan-impact.png`と`test-results/replan-decision-mobile.png`で確認した。

OS最大化・最小化の既存実機1件は、利用者側で同時にバックグラウンドテストを実行していた状態では合成終了要求が完了せず、今回の全件実行から除外した。通常サイズの変更・終了・再起動による復元は同じ最終実機回帰内で成功した。配布版は開発サーバーなしで起動し、同梱画面・SQLite読込・通常終了を確認したが、最大化を含む配布版の単独検証は合成終了要求が完了せず未実施とした。v0.4.12からの更新は、ロック中の拒否、破損の拒否、原子的置換、旧exeの控え、再実行、既存データ保持が成功した。公開デモの本番ビルドと保存・再読込のスモークテストも成功した。

SHA-256：exe 02FABF80FE1DB2956E2EA8121201076700EBA4F629C267C8840A3D3FB9D77BF7、ZIP CE36976FE4D4CE5A4FC6E48D660757FB0CB32F8D36B27CE2BDAB30C4F4BD8122。

## 再計画後の進捗通知・差分表示（2026-09-22）

単体23ファイル247件、型検査、ESLint、本番Webビルドが成功。進捗基準を承認済み計画から判定し、訂正・再訂正・取消、候補作成のみ、破棄、基準取り込み、JSON再読込を検証した。比較関数では同量の日付移動、周回終了位置、他教材への波及、未配置の増減、新規配置、変更なし、比較開始時刻、入力不変性と決定性を確認した。

検証専用SQLiteとWebView2を使った対象3シナリオが最終一括実行で成功。予定超過30問から20問・25問への訂正、候補作成・破棄時の通知維持、承認後と再起動後の通知消失、その記録の取消後の再計画・承認・再起動を確認した。最初の実行は候補保存前に未報告確認を調べるテスト側の待機不足で失敗したため、保存された候補を待つよう修正して再実行した。

標準画面と480px幅・文字間隔変更時のスクリーンショットを目視確認。変更した教材、周回、日別増減、未配置が表示され、詳細は既定で閉じる。キーボードで詳細を開き、横方向のはみ出しがないことも確認した。画像はtest-results/replan-impact.png、test-results/replan-decision-mobile.png。今回、実機全シナリオ・全テーマのaxe再検査・配布exeの再ビルドは未実施。Rust・保存形式・計画計算・前倒し処理は変更していない。

## v0.4.14 配布（2026-09-22）

Windows配布版のビルドに成功。v0.4.13からの更新用ZIPは、検証専用フォルダーでロック中・破損の拒否、原子的置換、旧exeの控え、再実行、データ保持を確認した。機能検証は直前節の単体247件と検証専用SQLiteでの実機3件を使用。通常利用のデータを開く配布exeの起動確認は今回実施していない。
# 2026-09-24：今後の予定の実績・不足表示とカレンダー整理

`6db4ed8`を基点に変更。計画生成・保存形式を変えず、既存の日別基準・承認済み履歴と有効な実績を共通集計する。過去日の確認済み不足と未報告を分ける純粋な集計を追加した。UI Skillsのlayout/typography/accessibility/colors/writing/UI方針に沿って実装担当が画面と差分を確認した。今回の変更には独立レビューを実施していない。

- 単体テスト325件（33ファイル）成功。予定10に対する実績未報告・0・6・10・12を過去／今日／未来で比較する20件の表示回帰ケースを追加。固定・可動への重複加算、取消、再配分で消えた日、基準なし、報告済み教材だけの不足集計を含む。
- 型検査、lint、通常・デモビルド成功。従来からの依存ライブラリ注釈と500KB超チャンクのビルド警告は残る。
- `scripts/future-progress-smoke.mjs`：隔離したブラウザーで1280/390/320pxを実操作。実績の追加、訂正、取消、再読込、再配分後も過去の分母10を保持、通常の「今日の予定」画面の維持、カレンダーの常設パネル除去、日付選択・閉じる・フォーカス復帰、表示切替、戻るを確認。
- mint/sky/lime × light/darkの6組合せで対象領域のaxe違反なし。文字200%＋字間・行間変更でも横方向のはみ出しなし。数量と不足を色だけに頼らず読めることを画像で確認した。
- 既存`calendar-quantity-smoke.mjs`と`navigation-result-smoke.mjs`を回帰実行。選択した週・スクロール位置・操作元フォーカス、混在単位、基準欠損、記録詳細の導線を確認。
- 検証専用SQLiteでTauri実機E2E「今日に5問記録すると翌日15問に調整され、SQLite再起動後も残る」成功。実績訂正・取消・超過・再起動と日別基準保持を確認。

画像：`test-results/future-progress/future-390.png`、`future-1280.png`、`calendar-1280.png`、`calendar-320.png`、`expanded-320.png`。通常データと公開デモの保存領域には触れていない。

未検証：全ネイティブE2Eの一括再実行、配布EXEの起動、スクリーンリーダーの実聴。axeのみでWCAG準拠とは断定しない。今回はリリース・公開更新を行っていない。

# v0.4.16：未記録実績の0表示

学習量の未記録実績を0問へ変更。予定・記録がない日は実績0を示すが、過去の予定基準が不明なら「基準なし」は維持する。保存上の未報告フラグと明示0の区別は残し、表示時に記録は生成しない。追加2件を含む305単体テスト、型検査、lint、通常・デモビルドを通過。

0表示の最終画面を1280/390/320pxで再操作し、全6テーマのaxe・文字拡大・戻る位置・混在単位を通過。Windows releaseビルドが成功し、実行ファイルのProductVersion/FileVersionが0.4.16であること、更新ZIPの内容とSHA-256を確認した。

配布版はdebug専用の`STUDYPLAN_TEST_DATA_DIR`を無視するため、通常データを守るため配布EXEの直接起動試験は未実施。既存release-smokeの説明を修正し、別Windowsテストアカウントでの明示指定なしでは起動しないようにした。隔離SQLiteを使ったTauri debug実機・再起動試験は下記のとおり実施済み。

# 2026-09-24：週間予定と詳細カレンダーの学習量

対象は共有済み`895afe8`に今回の変更を加えた状態。通常利用のDBを使わず、ブラウザーの新規contextと`.test-data/native-*`の検証用SQLiteで確認した。最終fetchでもorigin/mainに追加コミットなし。

| 検証 | 結果 |
| --- | --- |
| `node node_modules/vitest/vitest.mjs run` | 31ファイル・303件成功 |
| `node node_modules/typescript/bin/tsc -b` | 成功 |
| `node node_modules/eslint/bin/eslint.js . --max-warnings 0` | 成功 |
| `node node_modules/vite/bin/vite.js build` | 通常ビルド成功 |
| `node node_modules/vite/bin/vite.js build --mode demo --outDir .test-data/calendar-preview` | 隔離デモビルド成功 |
| `cargo build --manifest-path src-tauri/Cargo.toml --locked` | Rust debugビルド成功 |
| `node scripts/calendar-quantity-smoke.mjs` | 1280/390/320pxで実操作成功 |
| `node scripts/daily-flow-smoke.mjs http://127.0.0.1:4175/studyplan/` | 既存13シナリオ成功 |
| `node scripts/navigation-result-smoke.mjs http://127.0.0.1:4175/studyplan/` | 戻る・フォーカス・訂正取消・保存失敗の既存シナリオ成功 |
| `node node_modules/@playwright/test/cli.js test -g '実機：今日に5問記録すると翌日15問に調整され、SQLite再起動後も残る'` | Tauri/WebView2/SQLiteの1複合シナリオ成功 |

新しい純粋処理の試験は、別教材・別周回の超過を相殺しない集計、問とページの分離、取消除外、0と未記録、再配分後の過去不足、計画復元とJSON往復、旧データの基準なし、後日承認・候補の除外、ローカル承認日、日曜・土曜・月年境界を含む。バックアップの任意項目往復と不正な負数の拒否も追加した。

画面では、詳細入口の位置、週送り、選択週・スクロール・フォーカスを保つ戻る操作、内容／学習量で対象日を保つこと、表示切替が計画・実績を書き換えないこと、保存された過去20問対12問の不足8問、明示0、取消除外、基準なし、混在単位、教材を引き継ぐ記録導線を確認した。テキスト200%＋行間・字間変更でも横スクロールなし。キーボードで切替でき、フォーカス枠を確認した。mint/sky/lime×light/darkの描画完了後にmain領域へaxeを実行し、対象タグの違反なし。テーマ変更は有効テーマ属性を切り替えて測定し、設定画面のテーマ選択自体の回帰操作は今回の対象外。

ネイティブ試験では日別の確定基準が記録時に保存され、プロセスを閉じて起動し直しても保持され、数量が予定10・実績5・残り5になることを追加確認した。続く訂正・取消、0・予定どおり・超過記録、予定外教材、通常終了からの再起動も通過した。初回は週表示が複数日になったことで既存テストの曖昧なlocatorが失敗したため、対象日を明示して再実行した。

画像を確認：`test-results/calendar-quantity/quantity-1280.png`、`viewport-1280.png`、`viewport-390.png`、`quantity-320.png`、`expanded-320.png`。独立レビューの範囲と結果は[calendar-quantity-review.md](calendar-quantity-review.md)に記載。

未検証：ネイティブE2E全件の一括実行、スクリーンリーダーの実聴、配布インストーラー、Codex再起動後のSkill選択候補。axeのみでWCAG適合を断定していない。通常/デモのビルドには既存依存Zodの注釈と500KB超チャンクの警告が出るが、ビルドは成功した。旧データで根拠を復元できない日付は「基準なし」とする仕様上の制約がある。

## v0.4.17 配布準備（2026-09-24）

通常フロントエンドとTauri Windows releaseビルド成功。実行ファイルの製品・ファイルバージョンは0.4.17。v0.4.16の複製から更新用ZIPを試験し、ロック・破損拒否、置換、旧版退避、再実行、データ保持を確認した。機能検証は直前の325件と隔離したブラウザー・実機試験を使用。配布EXE自体の起動は、隔離Windowsアカウントで未実施。通常利用データにはアクセスしていない。

## v0.4.18：過去の予定量参照（2026-09-24）

開始時に同趣旨の未コミット5ファイルが存在し、それらを保護・確認して検証した。原因はhistoricalDayBaselineの承認日時必須・from以降必須の条件で、旧版の確定計画や再計画前から保持された過去予定を除外していたこと。SQLiteのstate.dataにplan/history/recordsを保存する実装と、再計画時の過去session保持・history退避を確認した。標準保存先をreadOnlyで確認したがrevision 1・計画0・記録0で、利用者の15問の事例そのものは確認できなかった。

327件の単体テスト、型検査、ESLint、通常・デモビルド成功。旧版承認日時なし・保持済み過去予定・JSON再読込、6/10・10/10・12/10・明示0・未報告・復元不能15問と未報告・訂正・取消を確認。新規保存機構は追加していない。既存の保存済み日別予定、確定計画・履歴、進捗反映前の数量を使用し、取消済み記録は除外する。

実画面は隔離ブラウザーの1280/390/320pxで、訂正・取消・再読込・6テーマのaxe・文字拡大を確認。配布EXE起動、全ネイティブE2E、スクリーンリーダー実聴は未実施。通常利用データの変更なし。ビルドの既存Zod注釈・チャンクサイズ警告は残る。

Tauri releaseビルドと製品バージョン0.4.18を確認。v0.4.17からの更新用ZIPは隔離した複製でロック・破損拒否、旧版退避、再実行、データ保持の試験に成功。
